# Dependencies (none committed — no-build vanilla ES modules; astronomy-engine
# is vendored directly under src/vendor/).
node_modules/

# Secrets / local
.env
*.local

# Deploy bundle (assembled by scripts/stage-dist.mjs at deploy time)
dist/

# OS / editor cruft
.DS_Store
